%SISTEMI
%sistema s[x(t)] =y(t)

%linearità
%	s[ax1(t)+bx2(t)]= a . y1(t)+ b . y2(t)

%t-invarianza
%	s[x(t-t0)] = y(t-t0)

%causalità
%Dato x(t+to):
%se to<=0 → causale
%se to> 0 → anticausale
%forma x(t+to).x(t-to)   → non causale

%memoria
%Dato x(t+to):
%se to=0 → senza memoria
%se to≠0 → con memoria

%stabilità
%	|x(t)|<L → |y(t)|<M
